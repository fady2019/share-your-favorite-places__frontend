import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'share-your-favorite-places__frontend',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
